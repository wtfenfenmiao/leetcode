不能正常加减乘除的各种骚操作......  （善用移位）

符号位用异或，对于掩码等等操作要学会怎么用

蜜汁各种溢出  
（一个办法是，INT_MAX和INT_MIN提前判断，就是不要到这里再判断，提前判断，比如每一步都是temp=temp*10+...，就不要*10了再和INT_MAX啥的判断，提前和INT_MAX/10判断。不要和INT_MAX判断，和INT_MAX/10这种提前比较）
（还可以用long）
（注意INT_MIN比INT_MAX要大一个数）

